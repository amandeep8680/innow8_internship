Django REST Framework — User & Group Permissions with DjangoModelPermissions

This topic is about authorization.

Authentication answers:

“Who are you?”

Permissions answer:

“What are you allowed to do?”

In Django REST Framework (DRF), DjangoModelPermissions connects DRF permissions with Django’s built-in permission system.







## working directory
projects/web/__drf__/user_group_permission